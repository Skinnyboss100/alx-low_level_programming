#include <stdio.h>

/**
 * main - A program that prints all the numbers of base 16 in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int 1;

	for(i = 48; i<58; i++)
	{
		putchar(i);
	}
	for (i = 97; i < 103; 1++)
	{
		putchar(i);
	}
	putchar('\n');
	return(0);
}
